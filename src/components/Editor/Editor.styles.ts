import styled from '@emotion/styled'

export const Textarea = styled.textarea`
    resize: none;
    background: transparent;
    width: 100%;
    height: 100%;
    border: none;
    outline: none;
    overflow: auto;
`
